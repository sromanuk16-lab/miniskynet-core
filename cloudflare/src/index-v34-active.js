import app from "./index-v331-active.js";

const VERSION = "v3.4-active-target-resolver-2026-07-03";
const JSON_HEADERS = { "content-type": "application/json; charset=utf-8" };
const DEFAULT_REPO = "sromanuk16-lab/miniskynet-core";
const DEFAULT_BRANCH = "main";

function json(data, status = 200) { return new Response(JSON.stringify(data, null, 2), { status, headers: JSON_HEADERS }); }
function now() { return new Date().toISOString(); }
function clip(s, n = 3900) { return String(s || "").slice(0, n); }
function compact(s) { return String(s || "").toLowerCase().replace(/\s+/g, " ").trim(); }
function cleanPath(path) { return String(path || "").trim().replace(/^\/+/, ""); }
function safePath(path) { const p = cleanPath(path); return (!p || p.includes("..") || p.length > 180 || !/^[a-zA-Z0-9_./-]+$/.test(p)) ? null : p; }
function encodePath(path) { return cleanPath(path).split("/").map(encodeURIComponent).join("/"); }
function decodeBase64Utf8(s) { const bin = atob(String(s || "").replace(/\n/g, "")); return new TextDecoder("utf-8").decode(Uint8Array.from(bin, c => c.charCodeAt(0))); }
function parseUpdate(update) { const msg = update?.message || update?.edited_message || null; if (!msg) return null; const text = String(msg.text || "").trim(); let command = null, args = ""; if (text.startsWith("/")) { const i = text.indexOf(" "); command = (i === -1 ? text : text.slice(0, i)).replace(/@\w+$/, "").toLowerCase(); args = i === -1 ? "" : text.slice(i + 1).trim(); } return { chatId: msg.chat?.id, userId: msg.from?.id, text, command, args }; }
async function kvGet(env, key, fallback) { const raw = await env.MINISKYNET_KV.get(key); if (!raw) return fallback; try { return JSON.parse(raw); } catch (_) { return fallback; } }
async function kvPut(env, key, value) { await env.MINISKYNET_KV.put(key, JSON.stringify(value, null, 2)); }
async function kvGetText(env, key) { return String(await env.MINISKYNET_KV.get(key) || "").trim(); }
async function getProposals(env) { return (await kvGet(env, "proposals", { proposals: [] })).proposals || []; }
async function saveProposals(env, proposals) { await kvPut(env, "proposals", { proposals: proposals.slice(-50) }); }
async function loadTelegramConfig(env) { const cfg = { TELEGRAM_BOT_TOKEN: String(env.TELEGRAM_BOT_TOKEN || "").trim(), TELEGRAM_ALLOWED_USER_ID: String(env.TELEGRAM_ALLOWED_USER_ID || "").trim() }; if (!cfg.TELEGRAM_BOT_TOKEN) cfg.TELEGRAM_BOT_TOKEN = await kvGetText(env, "config:TELEGRAM_BOT_TOKEN"); if (!cfg.TELEGRAM_ALLOWED_USER_ID) cfg.TELEGRAM_ALLOWED_USER_ID = await kvGetText(env, "config:TELEGRAM_ALLOWED_USER_ID"); return cfg; }
async function loadRepoConfig(env) { return { token: String(env.GITHUB_TOKEN || await kvGetText(env, "config:GITHUB_TOKEN") || "").trim(), repo: String(env.GITHUB_REPO || await kvGetText(env, "config:GITHUB_REPO") || DEFAULT_REPO).trim(), branch: String(env.GITHUB_BRANCH || await kvGetText(env, "config:GITHUB_BRANCH") || DEFAULT_BRANCH).trim() }; }
function isOwner(cfg, userId) { const owner = String(cfg.TELEGRAM_ALLOWED_USER_ID || "").trim(); return !owner || String(userId || "") === owner; }
async function tg(cfg, method, body) { const res = await fetch(`https://api.telegram.org/bot${cfg.TELEGRAM_BOT_TOKEN}/${method}`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(body) }); return await res.json().catch(() => ({})); }
async function send(cfg, chatId, text) { if (cfg.TELEGRAM_BOT_TOKEN && chatId) return await tg(cfg, "sendMessage", { chat_id: chatId, text: clip(text) }); }
async function githubFile(repoCfg, path) { const p = safePath(path); if (!p) throw new Error("unsafe_path"); const url = `https://api.github.com/repos/${repoCfg.repo}/contents/${encodePath(p)}?ref=${encodeURIComponent(repoCfg.branch)}`; const headers = { accept: "application/vnd.github+json", "user-agent": "MiniSkynet-Core" }; if (repoCfg.token) headers.authorization = `Bearer ${repoCfg.token}`; const res = await fetch(url, { headers }); const data = await res.json().catch(() => ({})); if (!res.ok) throw new Error(`GitHub ${res.status}: ${data?.message || "request failed"}`); if (Array.isArray(data)) throw new Error("path_is_directory"); if (!data.content) throw new Error("no_file_content"); const content = decodeBase64Utf8(data.content); return { path: p, sha: data.sha || "", size: data.size || content.length, content }; }
function findProposal(list, id) { const key = String(id || "").trim(); return list.find(p => p.id === key || String(p.id || "").startsWith(key)) || null; }
function dirname(path) { const a = cleanPath(path).split("/"); a.pop(); return a.join("/"); }
function joinRel(basePath, rel) { return `${dirname(basePath)}/${cleanPath(rel)}`; }
function forwarderTarget(file) { const m = String(file?.content || "").trim().match(/^export\s+\{\s*default\s*\}\s+from\s+["']\.\/([^"']+)["'];?$/); return m ? joinRel(file.path, m[1]) : null; }
function activeMainFromWrangler(content) { const m = String(content || "").match(/^main\s*=\s*["']([^"']+)["']/m); if (!m) return null; const rel = cleanPath(m[1]); return rel.startsWith("cloudflare/") ? rel : `cloudflare/${rel}`; }
async function resolveActiveTarget(env) { const repoCfg = await loadRepoConfig(env); const wrangler = await githubFile(repoCfg, "cloudflare/wrangler.toml"); const start = activeMainFromWrangler(wrangler.content); if (!start) throw new Error("wrangler main not found"); const chain = []; let path = start; for (let i = 0; i < 8; i++) { const file = await githubFile(repoCfg, path); const next = forwarderTarget(file); chain.push({ path: file.path, sha: file.sha, size: file.size, forward_to: next }); if (!next) return { repoCfg, wrangler, start, effective: file, chain }; path = next; } throw new Error("active target chain too deep"); }
function requestLooksRuntimeVisible(p) { const s = compact(`${p.title || ""} ${p.request || ""} ${p.summary || ""} ${p.description || ""} ${p.patch_draft?.intent || ""}`); return /help|status|stage|command|команд|статус|стейдж|development/.test(s); }
function splitNoFinalNewline(s) { return String(s || "").replace(/\n$/, "").split("\n"); }
function normalizeFinalNewline(text, reference) { let x = String(text || ""); if (reference.endsWith("\n") && !x.endsWith("\n")) x += "\n"; if (!reference.endsWith("\n") && x.endsWith("\n")) x = x.replace(/\n+$/, ""); return x; }
function buildFullFileUnifiedDiff(path, oldContent, newContent, oldSha = "old", newSha = "new") { const oldLines = splitNoFinalNewline(oldContent); const newLines = splitNoFinalNewline(newContent); return [`diff --git a/${path} b/${path}`, `index ${String(oldSha).slice(0, 7)}..${String(newSha).slice(0, 7)} 100644`, `--- a/${path}`, `+++ b/${path}`, `@@ -1,${oldLines.length} +1,${newLines.length} @@`, ...oldLines.map(l => `-${l}`), ...newLines.map(l => `+${l}`)].join("\n") + "\n"; }
function applyUnifiedDiff(original, diff) { const finalNl = original.endsWith("\n"); const src = original.replace(/\n$/, "").split("\n"); const lines = String(diff || "").replace(/\r\n/g, "\n").split("\n"); const out = []; let srcIndex = 0; let saw = false; for (let i = 0; i < lines.length; i++) { const h = lines[i].match(/^@@\s+-(\d+)(?:,(\d+))?\s+\+(\d+)(?:,(\d+))?\s+@@/); if (!h) continue; saw = true; const oldStart = parseInt(h[1], 10) - 1; while (srcIndex < oldStart) out.push(src[srcIndex++]); i++; for (; i < lines.length; i++) { const l = lines[i]; if (l.startsWith("@@ ")) { i--; break; } const body = l.slice(1); if (l.startsWith(" ")) { if (src[srcIndex] !== body) throw new Error(`diff context mismatch near source line ${srcIndex + 1}`); out.push(src[srcIndex++]); } else if (l.startsWith("-")) { if (src[srcIndex] !== body) throw new Error(`diff removal mismatch near source line ${srcIndex + 1}`); srcIndex++; } else if (l.startsWith("+")) out.push(body); } } if (!saw) throw new Error("no unified diff hunk found"); while (srcIndex < src.length) out.push(src[srcIndex++]); return out.join("\n") + (finalNl ? "\n" : ""); }
function deterministicNewContent(p, file) { const s = compact(`${p.title || ""} ${p.request || ""} ${p.summary || ""} ${p.patch_draft?.intent || ""}`); if (!(/help|команд/.test(s) && /stage|development|стейдж|статус/.test(s))) return null; const marker = '"/start /help /status",'; if (!file.content.includes(marker)) return null; const line = '      "Development stage: " + VERSION,'; if (file.content.includes(line.trim())) throw new Error("development stage line already exists"); return file.content.replace(marker, `${marker}\n${line}`); }
async function patchAutoTarget(env, id) { const proposals = await getProposals(env); const p = findProposal(proposals, id); if (!p) throw new Error(`proposal ${id} not found`); const active = await resolveActiveTarget(env); p.file_path = active.effective.path; if (p.patch_draft) { p.patch_draft.target_file = active.effective.path; p.patch_draft.current_sha = active.effective.sha; p.patch_draft.retargeted_at = now(); } delete p.code_draft; delete p.code_draft_validation; p.target_guard = { mode: "active_target_resolver", checked_at: now(), start: active.start, effective: active.effective.path, chain: active.chain }; await saveProposals(env, proposals); return { p, active }; }
async function generateCodeDraft(env, id) { const proposals = await getProposals(env); const p = findProposal(proposals, id); if (!p) throw new Error(`proposal ${id} not found`); const active = await resolveActiveTarget(env); if (requestLooksRuntimeVisible(p) && p.patch_draft?.target_file !== active.effective.path) throw new Error(`target is ${p.patch_draft?.target_file}; active target is ${active.effective.path}. Run /patch_auto_target ${p.id}`); if (!p.patch_draft) throw new Error("patch_draft missing: run /patch_preview first"); const file = active.effective; if (p.patch_draft.current_sha && p.patch_draft.current_sha !== file.sha) throw new Error("patch_draft sha differs from active target sha; run /patch_preview after /patch_auto_target"); let newContent = deterministicNewContent(p, file); if (!newContent) throw new Error("lightweight operation not found for active target"); newContent = normalizeFinalNewline(newContent, file.content); const diff = buildFullFileUnifiedDiff(file.path, file.content, newContent, file.sha, "new"); const patched = applyUnifiedDiff(file.content, diff); if (patched !== newContent) throw new Error("internal validator failed"); p.code_draft = { created_at: now(), target_file: file.path, current_sha: file.sha, summary: "Добавить отображение текущего development stage в /help.", risk: "low — меняется только текст help-команды", unified_diff: diff, test_plan: ["/status", "/help"], mode: "active_target_lightweight_no_write" }; p.code_draft_validation = { checked_at: now(), target_file: file.path, current_sha: file.sha, active_main: active.effective.path, ok: true, blockers: [], warnings: [] }; p.status = "code_draft_ready"; await saveProposals(env, proposals); return p; }
async function applyCheck(env, id) { const proposals = await getProposals(env); const p = findProposal(proposals, id); if (!p) throw new Error(`proposal ${id} not found`); const active = await resolveActiveTarget(env); const blockers = []; const warnings = []; if (!p.code_draft) blockers.push("code_draft missing"); if (p.code_draft?.target_file !== active.effective.path) blockers.push(`code_draft targets ${p.code_draft?.target_file}, active target is ${active.effective.path}`); if (p.code_draft?.current_sha !== active.effective.sha) blockers.push("code_draft sha differs from active target sha"); let patched = null; if (!blockers.length) { try { patched = applyUnifiedDiff(active.effective.content, p.code_draft.unified_diff); if (patched === active.effective.content) warnings.push("patch produces identical content"); } catch (e) { blockers.push(`diff does not apply: ${e.message || e}`); } } p.code_draft_validation = { checked_at: now(), target_file: active.effective.path, current_sha: active.effective.sha, active_main: active.effective.path, ok: blockers.length === 0, blockers, warnings }; await saveProposals(env, proposals); return { p, active, blockers, warnings, patched }; }
function activeTargetText(a) { return [`🎯 Active target:`, `- wrangler main: ${a.start}`, `- effective: ${a.effective.path}`, `- sha: ${String(a.effective.sha).slice(0, 12)}`, "", "Chain:", ...a.chain.map((x, i) => `${i + 1}. ${x.path}${x.forward_to ? ` → ${x.forward_to}` : ""}`)].join("\n"); }
function draftText(p) { const d = p.code_draft || {}; return [`🪶 Active-target code draft ${p.id}:`, `- target: ${d.target_file}`, `- sha: ${String(d.current_sha || "").slice(0, 12)}`, `- validation: ${p.code_draft_validation?.ok ? "valid ✅" : "unknown"}`, `- mode: ${d.mode}`, "", `Summary: ${d.summary}`, "", "Unified diff:", clip(d.unified_diff || "", 1800), d.unified_diff && d.unified_diff.length > 1800 ? "\n...truncated. Полностью: /code_show prop_id" : "", "", "Код в GitHub НЕ изменён."].join("\n"); }
function checkText(r) { return [`🔐 Active apply check ${r.p.id}:`, `- target: ${r.active.effective.path}`, `- sha: ${String(r.active.effective.sha).slice(0, 12)}`, `- code approved: ${r.p.code_approved_at ? "yes ✅" : "no ⛔"}`, `- diff applies: ${r.patched ? "yes ✅" : "no/blocked"}`, "", r.blockers.length ? "Blockers:" : "Blockers: none", ...r.blockers.map(x => `- ${x}`), r.warnings.length ? "\nWarnings:" : "\nWarnings: none", ...r.warnings.map(x => `- ${x}`), "", r.blockers.length ? "Next: исправь blockers" : r.p.code_approved_at ? "Next: apply still handled by guarded layer" : `/code_approve ${r.p.id}`].join("\n"); }
const COMMANDS = new Set(["/active_target", "/patch_auto_target", "/code_preview", "/code_check", "/apply_check", "/status", "/help"]);
async function handleCommand(env, cfg, msg) { const { chatId, command, args } = msg; try { if (command === "/active_target") return await send(cfg, chatId, activeTargetText(await resolveActiveTarget(env))); if (command === "/patch_auto_target") { if (!args) return await send(cfg, chatId, "Формат: /patch_auto_target prop_id"); const r = await patchAutoTarget(env, args); return await send(cfg, chatId, [`✅ Auto-target ${r.p.id}:`, `- effective: ${r.active.effective.path}`, `- sha: ${String(r.active.effective.sha).slice(0, 12)}`, "", `Теперь: /patch_preview ${r.p.id}`].join("\n")); } if (command === "/code_preview") { if (!args) return await send(cfg, chatId, "Формат: /code_preview prop_id"); return await send(cfg, chatId, draftText(await generateCodeDraft(env, args))); } if (command === "/code_check" || command === "/apply_check") { if (!args) return await send(cfg, chatId, `Формат: ${command} prop_id`); return await send(cfg, chatId, checkText(await applyCheck(env, args))); } if (command === "/status") return await send(cfg, chatId, [`📡 MiniSkynet v3.4 status`, `- version: ${VERSION}`, `- active target resolver: active`, `- auto target: /patch_auto_target`, `- write/apply remains guarded by lower layer`, `- commands: /active_target /patch_auto_target /code_preview /apply_check`].join("\n")); if (command === "/help") return await send(cfg, chatId, ["/start /help /status", "/active_target — показать реальный активный файл", "/patch_auto_target prop_id — перенести proposal на active target", "/code_preview prop_id — lightweight draft по active target", "/code_check prop_id /apply_check prop_id"].join("\n")); } catch (e) { return await send(cfg, chatId, `❌ v3.4 error: ${clip(e.message || e, 900)}`); } }
export default { async fetch(request, env, ctx) { const url = new URL(request.url); if (url.pathname === "/" || url.pathname === "/health") return json({ ok: true, version: VERSION, active_target_resolver: true }); if (url.pathname === "/telegram" && request.method === "POST") { const cfg = await loadTelegramConfig(env); const update = await request.clone().json().catch(() => null); const msg = update ? parseUpdate(update) : null; if (msg && COMMANDS.has(msg.command)) { if (!isOwner(cfg, msg.userId)) { await send(cfg, msg.chatId, "⛔ Доступ закрыт.").catch(() => null); return json({ ok: true, denied: true }); } ctx.waitUntil(handleCommand(env, cfg, msg).catch(err => send(cfg, msg.chatId, `❌ v3.4 internal error: ${clip(err, 400)}`).catch(() => null))); return json({ ok: true, active_target_resolver: true }); } } return app.fetch(request, env, ctx); }, async scheduled(event, env, ctx) { return app.scheduled(event, env, ctx); } };
